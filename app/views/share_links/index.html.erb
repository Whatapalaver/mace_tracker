<% content_for :title, "Share Links" %>

<div class="mb-4 flex items-center justify-between">
  <h1 class="text-2xl font-semibold tracking-tight">Share links</h1>
  <%= link_to "New link", new_share_link_path, class: "text-sm font-medium text-rust hover:text-rust-dark" %>
</div>

<% if @share_links.empty? %>
  <p class="text-sm text-muted">No share links yet. Create one to let your coach view your progress.</p>
<% else %>
  <ul class="space-y-3">
    <% @share_links.each do |share_link| %>
      <li class="rounded-lg border border-border bg-surface p-4 text-sm">
        <p class="font-medium"><%= share_link.description %></p>
        <p class="mt-1 break-all font-mono text-xs text-rust"><%= shared_dashboard_url(token: share_link.token) %></p>
        <p class="mt-1 text-xs text-muted">
          <% if share_link.expires_at.present? %>
            <% if share_link.expired? %>
              Expired <%= share_link.expires_at.to_fs(:long) %>
            <% else %>
              Expires <%= share_link.expires_at.to_fs(:long) %>
            <% end %>
          <% else %>
            Permanent
          <% end %>
        </p>
        <div class="mt-2 flex gap-3">
          <%= button_to "Regenerate", regenerate_share_link_path(share_link), method: :patch, class: "text-xs font-medium text-rust hover:text-rust-dark" %>
          <%= button_to "Revoke", share_link_path(share_link), method: :delete,
                data: { turbo_confirm: "Revoke this share link? The URL will stop working immediately." },
                class: "text-xs font-medium text-crimson hover:text-crimson/80" %>
        </div>
      </li>
    <% end %>
  </ul>
<% end %>
